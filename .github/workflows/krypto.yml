name: Krypto

on: [push]

jobs:
  krypto:
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout krypto
        uses: antoniouaa/krypto@v1
        with:
          directory: "."
        env:
          TOKEN_GITHUB: ${{ secrets.GITHUB_TOKEN }}
